name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Build with Docker
      run: docker-compose -f $GITHUB_REPOSITORY/actions.yml build
